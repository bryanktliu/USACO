package USACO;

import java.io.*;

public class googleClient {

    public static void main(String[] args) throws IOException {
        /**
         * String userHome = System.getProperty("user.home"); Socket socket = new
         * Socket("www.google.com", 80); InputStream input = socket.getInputStream(); try
         * (BufferedWriter out = new BufferedWriter(new
         * OutputStreamWriter(socket.getOutputStream()))) { out.write("GET / HTTP/1.0\r\n\r\n");
         * out.flush(); String line = IOUtils.readLines(); if(line == null) { throw new
         * IOException("Something is wrong"); } else { String[] rline = line.split(" ", 3);
         * if(rline[1].equals("200")) { while((line = in.readLine()) != null) { if(line.isEmpty()) {
         * break; } } } else { System.out.println(rline[2]); } } Path file =
         * Paths.get("/Users/frankliu/Downloads/google.txt"); Files.copy(input, file); }
         */
    }
}
